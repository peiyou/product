<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.btcc.web.report.website.mapper.RegisterUserMapper">

    <select id="getRegisterCountUserId" resultType="java.lang.Integer" parameterType="java.util.Map">
        select
          count(user_id) as registerUserNum
        from
          users
        where
          user_regdate >= unix_timestamp(#{startDate}) - 8*3600
          and user_regdate &lt; unix_timestamp(#{endDate}) - 8 *3600
    </select>

</mapper>